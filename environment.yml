name: pyds-env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.8
  - pip
  - pip:
    - codecov
    - pytest-cov
    - sphinx==5.0
    - sphinx-readable-theme==1.3.0
    - myst_nb==0.17.2
    - lcov
